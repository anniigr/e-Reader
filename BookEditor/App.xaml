<Application x:Class="BookEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BookEditor.ViewModels"
             xmlns:views="clr-namespace:BookEditor.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#FF4081"/>

            <DataTemplate DataType="{x:Type vm:WelcomeViewModel}">
                <views:WelcomeView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:BookListViewModel}">
                <views:BookListView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:BookDetailViewModel}">
                <views:BookDetailView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReaderViewModel}">
                <views:ReaderView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>